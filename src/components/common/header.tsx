import { ModeToggle } from "../mode-toggle";

export default function Header() {
  return (
    <header>
      <nav>
        <div>
          <ModeToggle />
        </div>
      </nav>
    </header>
  );
}
